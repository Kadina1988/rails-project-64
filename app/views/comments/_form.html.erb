<%= turbo_frame_tag @comment do %>
  <%= form_with model: [@post, @comment] do |f| %>
    <p>
      <%= f.label :content %>
      <%= f.text_field :content %>
    </p>
    <p><%= f.submit %></p>
  <% end %>
<% end %>
